plugins {
    id 'java-library'
    id 'kotlin'
    id 'signing'
    id 'maven-publish'
}

java {
    sourceCompatibility = JavaVersion.VERSION_1_7
    targetCompatibility = JavaVersion.VERSION_1_7
}

task javadocJar(type: Jar) {
    archiveClassifier.set("javadoc")
    from javadoc
}

task sourcesJar(type: Jar) {
    archiveClassifier.set('sources')
    from sourceSets.main.allSource
}

tasks.withType(Javadoc) {
    options.addStringOption('Xdoclint:none')
    options.addStringOption('encoding', 'UTF-8')
    options.addStringOption('charset', 'UTF-8')
}

artifacts {
    archives sourcesJar
    archives javadocJar
}

afterEvaluate{
    Properties properties = new Properties()
    properties.load(project.rootProject.file('local.properties').newDataInputStream())
    publishing {
        publications {
            mavenAndroid(MavenPublication) {
                groupId "io.github.neutrosntarer"
                artifactId "npc"
                version "1.0.0"
                artifact sourcesJar
                artifact javadocJar
                from components.java
                pom {
                    packaging = 'jar'
                    name = "npc"
                    description = "Near Procedure Call."
                    version "1.0.0"
                    url = "https://github.com/neutronstarer/npc_kotlin"
                    licenses {
                        license {
                            name = "BSD 3-Clause"
                            url = "https://github.com/neutronstarer/npc_kotlin/blob/main/LICENSE"
                        }
                    }
                    developers {
                        developer {
                            id = 'neutronstarer'
                            name = 'neutronstarer'
                            email = 'neutronstarer@gmail.com'
                        }
                    }
                    scm {
                        url = "https://github.com/neutronstarer/npc_kotlin"
                        connection = "scm:git:git@github.com:neutronstarer/npc_kotlin.git"
                        developerConnection = "scm:git:git@github.com:neutronstarer/npc_kotlin.git"
                    }
                }
            }
        }
        repositories {
            maven {
//            url = "https://s01.oss.sonatype.org/content/repositories/snapshots/"
                url = "https://s01.oss.sonatype.org/service/local/staging/deploy/maven2/"
                println properties.get("ossrh.username")
                println properties.get("ossrh.password")
                credentials {
                    username properties.get("ossrh.username")
                    password properties.get("ossrh.password")
                }
            }
        }
    }

    signing {
        sign publishing.publications.mavenAndroid
    }
}
